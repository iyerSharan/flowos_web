# macOS
.DS_Store
**/.DS_Store

# ── Electron build outputs ──────────────────────────────────────────────────
# App bundles (huge) — never commit these
FlowOS/dist/*/mac-universal/
FlowOS/dist/*/win-unpacked/
FlowOS/dist/*/*.blockmap
FlowOS/dist/*/builder-debug.yml
FlowOS/dist/*/latest*.yml

# Installers — too large for git (dmg=199 MB, exe=95 MB).
# Upload them to the GitHub Release v0.1.0 instead (see README steps below).
FlowOS/dist/**/*.dmg
FlowOS/dist/**/*.exe

# Node / dev
node_modules/
*.log

# Waitlist data — stored privately in iyerSharan/flowos-waitlist
waitlist.json

# Local decryption tool — never commit (keep off GitHub)
decrypt_waitlist.py

# Source design files (bundler output — compiled into index.html / business.html)
"FlowOS Marketing - Standalone-2.html"
"FlowOS Business - Standalone.html"
FlowOS Marketing - Standalone-2.html
FlowOS Business - Standalone.html
